language: generic

services:
  - docker

matrix:
  include:
  - name: "bionic melodic"
    dist: bionic

install:
  - docker build -t aisaac:rostest .

script:
  - docker run -it --rm --name aisaac_test aisaac:rostest
